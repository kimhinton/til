# itertools chain

`itertools.chain` flattens multiple iterables without creating intermediate lists.

```
from itertools import chain
all_items = list(chain(list1, list2, list3))
```

_Learned on 2025-05-15_
