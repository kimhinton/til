# walrus operator

The walrus operator `:=` assigns and returns a value in a single expression. Useful in while loops and comprehensions.

```
if (n := len(data)) > 10:
    print(f'{n} items')
```

_Learned on 2025-05-10_
