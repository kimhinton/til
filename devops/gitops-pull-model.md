# gitops pull model

GitOps pull model: the cluster watches the repo, CI never touches the cluster.

```
# ArgoCD detects drift between git state and cluster state
# Auto-syncs or alerts based on policy
```

_Learned on 2025-05-27_
